## odin (and other) subdirectory

This directory implements plague virulence simulations implemented in [odin]() (and other platforms, but for now odin is fastest). (The directory should also probably be renamed eventually during cleanup)

### model definitions

`odin` and `macpan2` both generate 
* `discrete_odin.R`: `odin` model definition file
