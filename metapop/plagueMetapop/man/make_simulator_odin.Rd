% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrete_macpan2.R, R/discrete_odin.R,
%   R/discrete_pureR.R
\name{make_simulator_macpan2}
\alias{make_simulator_macpan2}
\alias{run_simulator_macpan2}
\alias{conv_macpan2}
\alias{make_simulator_odin}
\alias{run_simulator_odin}
\alias{conv_odin}
\alias{make_simulator_pureR}
\alias{run_simulator_pureR}
\alias{conv_pureR}
\title{Two-strain n-patch simulator interface (odin, macpan2, pureR backends)}
\usage{
make_simulator_macpan2(
  beta_vec = c(1.5, 2.5),
  K = 10000,
  r = 0.125,
  n_patch = 100,
  nt = 1000,
  alpha = 0.001,
  I_init = 10,
  strain2_delay = 0,
  seed = NULL
)

run_simulator_macpan2(x)

conv_macpan2(traj)

make_simulator_odin(
  beta_vec = c(1.5, 2.5),
  K = 10000,
  r = 0.125,
  n_patch = 100,
  nt = 1000,
  alpha = 0.001,
  I_init = 10,
  strain2_delay = 0,
  gamma = c(1, 1),
  dt = 1,
  def_file = "discrete_odin_def.R",
  gen_local = NULL
)

run_simulator_odin(x, chunk = 50L, stop_cond = NULL)

conv_odin(x, format = c("long", "wide"))

make_simulator_pureR(
  beta_vec = c(1.5, 2.5),
  K = 10000,
  r = 0.125,
  n_patch = 100,
  n_strain = 2,
  nt = 1000,
  alpha = 0.001,
  I_init = 10,
  strain2_delay = 0,
  seed = NULL
)

run_simulator_pureR(x)

conv_pureR(x)
}
\arguments{
\item{beta_vec}{length-2 vector of per-capita transmission rates}

\item{K}{carrying capacity (scalar or vector of length n_patch)}

\item{r}{host growth rate per disease generation (ditto)}

\item{n_patch}{number of patches}

\item{nt}{time steps}

\item{alpha}{between-patch transmission probability}

\item{I_init}{mean initial infected per patch (Poisson draw; length 1 or 2)}

\item{strain2_delay}{steps before strain 2 is seeded (0 = immediate)}

\item{seed}{PRNG seed}

\item{x}{simulator object from the corresponding \code{make_simulator_*()} function}

\item{gamma}{length-2 vector of per-strain recovery rates (euler models only)}

\item{dt}{time-step size in disease-generation units (euler models only)}

\item{def_file}{odin DSL filename in inst/odin/}

\item{gen_local}{pre-compiled odin generator; compiled via compile_odin() if NULL}

\item{chunk}{steps per chunk; only used when stop_cond is non-NULL}

\item{stop_cond}{NULL (run all nt steps) or a function(row) -> logical
called on the last row of each chunk; return TRUE to stop early.
See stop_either_extinct() and stop_both_extinct().}

\item{format}{"long" (default) or "wide"}

\item{n_strain}{number of strains [only 2 supported]}
}
\value{
an odin model instance with nt, gen, and init_args attributes
}
\description{
Three-function interface shared across all backends:
\itemize{
  \item \code{make_simulator_*()} constructs a simulator object
  \item \code{run_simulator_*()} runs it and returns raw output
  \item \code{conv_*()} converts raw output to a common long-format tibble
    with columns \code{step}, \code{state}, \code{patch}, \code{value}
}
}
