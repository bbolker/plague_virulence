% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrete_run.R
\name{traj_stats_ode}
\alias{traj_stats_ode}
\title{Epidemic transient summary statistics for a single-strain single-patch ODE run}
\usage{
traj_stats_ode(
  run,
  beta = NULL,
  gamma = NULL,
  K = NULL,
  r = NULL,
  logistic_growth = NULL
)
}
\arguments{
\item{run}{long-format tibble from \code{discrete_run()} with \code{n_patch = 1}
and \code{I_init = c(I0, 0)} (single strain)}

\item{beta}{transmission rate (\code{beta_vec[1]}); taken from \code{attr(run, "params")} if NULL}

\item{gamma}{recovery rate (\code{gamma[1]}); taken from \code{attr(run, "params")} if NULL}

\item{K}{carrying capacity; taken from \code{attr(run, "params")} if NULL}

\item{r}{host intrinsic growth rate; taken from \code{attr(run, "params")} if NULL}

\item{logistic_growth}{1 = logistic; 0 = linear restoring force; taken from \code{attr(run, "params")} if NULL}
}
\value{
named numeric vector with elements:
  \code{eq} (endemic equilibrium of I1),
  \code{T1} (first downward crossing of eq by I1: last step with I1 > eq before dip),
  \code{T2}, \code{I2} (time and value of first local minimum of I1,
    identified by the first index where diff(diff(I1)) > 0 after T1),
  \code{T3}, \code{I3} (time and value of first local minimum of S),
  \code{T4} (second upward crossing of eq by I1: last step with I1 < eq before recovery).
  Any statistic that cannot be found returns \code{NA}.
}
\description{
Epidemic transient summary statistics for a single-strain single-patch ODE run
}
