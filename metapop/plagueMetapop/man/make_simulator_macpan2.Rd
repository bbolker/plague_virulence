% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrete_macpan2.R
\name{make_simulator_macpan2}
\alias{make_simulator_macpan2}
\title{Build a macpan2 simulator for the two-strain n-patch model.
Simulator creation is separated from trajectory generation so the two
steps can be timed independently.}
\usage{
make_simulator_macpan2(
  beta_vec = c(1.5, 2.5),
  K = 10000,
  r = 0.125,
  n_patch = 100,
  nt = 1000,
  alpha = 0.001,
  I_init = 10,
  strain2_delay = 0,
  seed = NULL
)
}
\arguments{
\item{beta_vec}{length-2 vector of per-capita transmission rates}

\item{K}{carrying capacity (scalar or vector of length n_patch)}

\item{r}{host growth rate per disease generation (ditto)}

\item{n_patch}{number of patches}

\item{nt}{time steps}

\item{alpha}{between-patch transmission probability}

\item{I_init}{mean initial infected per patch (Poisson draw; length 1 or 2)}

\item{strain2_delay}{steps before strain 2 is seeded (0 = immediate)}

\item{seed}{PRNG seed (used for initial condition draws in R)}
}
\value{
a macpan2 simulator object
}
\description{
Build a macpan2 simulator for the two-strain n-patch model.
Simulator creation is separated from trajectory generation so the two
steps can be timed independently.
}
