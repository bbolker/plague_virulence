% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrete_run.R
\name{discrete_run}
\alias{discrete_run}
\title{Run one or more two-strain n-patch simulations across platforms}
\usage{
discrete_run(
  beta_vec = c(1.5, 2.5),
  K = 10000,
  r = 0.125,
  n_patch = 100,
  nt = 1000,
  alpha = 0.001,
  gamma = c(1, 1),
  strain2_delay = 0,
  I_init = 10,
  seed = NULL,
  nsim = 1,
  chunk = NULL,
  stop_cond = stop_both_extinct,
  def_file = "discrete_odin_def.R",
  dt = 1,
  platform = c("odin", "macpan2", "pureR")
)
}
\arguments{
\item{beta_vec}{length-2 vector of per-capita transmission rates (one per strain)}

\item{K}{carrying capacity (scalar or vector of length n_patch)}

\item{r}{host growth rate per disease generation (ditto)}

\item{n_patch}{number of patches}

\item{nt}{time steps}

\item{alpha}{between-patch transmission probability}

\item{gamma}{length-2 vector of per-strain recovery rates (euler models only)}

\item{strain2_delay}{steps before strain 2 is seeded (0 = immediate)}

\item{I_init}{mean initial infected per patch (Poisson draw; length 1 or 2)}

\item{seed}{PRNG seed}

\item{nsim}{number of simulations to run}

\item{chunk}{steps per chunk for early stopping (odin only; ignored if stop_cond is NULL)}

\item{stop_cond}{function(row) -> logical; called on the last row of each
chunk; return TRUE to halt early. Use stop_either_extinct() or
stop_both_extinct(), or supply a custom function.
NULL runs all nt steps. Only supported for platform = "odin".}

\item{def_file}{odin DSL filename in inst/odin/ (odin platform only)}

\item{dt}{time-step size in disease-generation units (euler models only)}

\item{platform}{simulation backend: "odin" (default), "macpan2", or "pureR"}
}
\value{
a single long-format tibble (nsim == 1) or a list of nsim tibbles
}
\description{
Run one or more two-strain n-patch simulations across platforms
}
\details{
Parallelism is controlled by the caller via future::plan() before
  invoking discrete_run(). Use plan(multisession, workers = N) for parallel
  runs; plan(sequential) (the default) for single-threaded execution.
}
\examples{
## stop as soon as either strain goes extinct (odin platform only):
## plan(multisession)
## runs <- discrete_run(nsim = 20, stop_cond = stop_either_extinct)
## sumfun_discrete(runs)

## custom condition — stop when strain 2 alone is gone:
## runs <- discrete_run(nsim = 20, chunk = 100,
##   stop_cond = function(row) sum(row[, grep(",2\\]$", colnames(row))]) == 0)
}
