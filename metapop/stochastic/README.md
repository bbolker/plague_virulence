* `simulation.R`: Yuyang's original sim code (didn't want to mess with this too much)
* `sim_batch0.R`: experiments paralleling `simulation.R` (but using `simulation_funs.R` instead)
* `simulation_funs.R`: functions for simulation, summarization, plotting
* `sim_batch1.R`: more extensive simulations (ranging over R0 and alpha)
* `plot_batch1.R`: report on `sim_batch1` results
* `sim_batch2.R`: simulations ranging over R0, alpha and K
* `plot_batch2.R`: report on `sim_batch1` results
* `outputs`: data and plots generated from the simulations 