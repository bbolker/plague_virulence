* improve sim speed by vectorizing (C++?)
* comparison with deterministic (2- and 3-param) models?
* test sensitivity to params other than R0, alpha?
* understand discrepancy between batch0 and batch1 (and base simulation runs from Yuyang)
   * aren't params equivalent?
   * save more intermediate info?
